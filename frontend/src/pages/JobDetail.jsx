// TODO: Implement Job Detail page — shows full JD, company info, Apply button
// Calls GET /api/jobs/:id and optionally GET /api/candidates/match/:id
export default function JobDetail() {
    return <div><h1>Job Detail</h1></div>;
}
